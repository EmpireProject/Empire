version: 2
jobs:
   build:
     docker:
       - image: circleci/python:2.7
     steps:
       - checkout
       - run:
            name: Run install.sh
            command: 'cd setup/ && ./install.sh'
